[build]
  command = "npm run build"
  publish = "dist/setly"

[build.environment]
  NODE_VERSION = "20"

# Angular SPA: serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
