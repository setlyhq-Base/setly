// Deprecated: AUTH_FLAGS replaced by environment.featureFlags.softDisableAuth.
// Keeping for backward compatibility; new code should read from environment.featureFlags.
export const AUTH_FLAGS = {
  AUTH_ENABLED: true
};
